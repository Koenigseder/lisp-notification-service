{
  "name": "lisp-notification-service",
  "version": "1.0.0",
  "description": "A small service used to notify people over the Lisp app if a list was changed",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^11.11.0"
  }
}
